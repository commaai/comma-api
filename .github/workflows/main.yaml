name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: '16'
        cache: 'pnpm'

    - run: pnpm install  # runs prepare script, which runs build
    - run: pnpm run lint
